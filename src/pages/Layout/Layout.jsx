import React from 'react'
import { QuestionCircleOutlined, SyncOutlined } from '@ant-design/icons';
import { AutoComplete, Avatar, Divider, FloatButton, Input, theme,Modal,Popover,Space,
  notification,Menu,Layout as LayoutAntd, List, Dropdown, Typography, Card,message as Mensaje } from 'antd';
const Layout = (props) => {
  return (
    <LayoutAntd
      style={{
        minHeight: '100vh',
      }}
    >
      {/* {contextHolder} */}
        
    </LayoutAntd>
  )
}

export default Layout